This directory will be populated with ssh config file for 'realtime' users.
The directory will be mounted as /home/realtime/.ssh/
This will make sure the docker instances will have the same authorized_keys
 if recreated.
