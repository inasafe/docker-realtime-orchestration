This directory will be populated with global ssh config file for open ssh
server.
The directory will be mounted as /etc/ssh/
This will make sure the docker instances will have the same server identity
 if recreated.
